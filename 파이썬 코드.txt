import sys
input = sys.stdin.readline


def swap(str1, op, str2):
    return str1+str2+op


def put_both(string):
    return '('+ string + ')'


def find_left_right(index):
    # 왼쪽부터 )가 있는지 확인, 오른쪽은 (
    # 왼쪽부터 탐색하는 이유는 양쪽에 *나 /가 있을 때는 왼쪽에 괄호를 쳐야되기 때문
    # *나 /기호 왼쪽에 ()가 있음
    if expression[index - 1] == ')':
        tmp_index = index - 1
        while 1:
            tmp_index -= 1
            if expression[tmp_index] == '(':
                return tmp_index, index + 1, 'left'
    # *나 /기호 오른쪽에 ()가 있음
    elif expression[index + 1] == '(':
        tmp_index = index + 1
        while 1:
            tmp_index += 1
            if expression[tmp_index] == ')':
                return index - 1, tmp_index, 'right'
    # *나 /기호 양 옆에 ()가 없음
    else:
        return index - 1, index + 1, 'none'


expression = input().rstrip()
# (를 result 에 insert
open_left = False
# 입력 받은 문자열에 이미 (가 존재하는지
is_left = False
for i in range(len(expression)):
    letter = expression[i]
    if letter == '*' or letter == '/':
        start, end, direct = find_left_right(i)

        start_to_end = expression[start:end + 1]
        result = expression.replace(start_to_end, put_both(start_to_end))



print(result)
"""
처음 생각
아스키 코드를 활용하면 좋을 것 같다.
*와 /가 나왔을 때 괄호로 먼저 감싸주어야 하기 때문에 *와 /가 중요해보인다.

"""
#(letter == ')' or
